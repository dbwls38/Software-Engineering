pipeline {
    agent any

    environment {
		JUNIT_JAR_URL = 'https://repo1.maven.org/maven2/org/junit/platform/junit-platform-console-standalone/6.0.3/junit-platform-console-standalone-6.0.3.jar'
        CLASS_DIR   = 'classes'
        REPORT_DIR  = 'test-reports'
        TEST_OUTPUT = 'test-output.txt'
        JUNIT_JAR   = 'lib/junit.jar'
    }

    stages {
        stage('Checkout') {
            steps {
                // GitHub 저장소에서 소스코드 가져오기
                checkout scm
            }
        }

        stage('Prepare') {
            steps {
                // 빌드 및 테스트 결과 저장할 폴더 생성
                sh '''
                    mkdir -p ${CLASS_DIR}
                    mkdir -p ${REPORT_DIR}
                    mkdir -p lib
                '''
            }
        }

        stage('Build') {
            steps {
                sh '''
                    echo "[*] Compiling source files..."
                    find Test/src -name "*.java" > sources.txt
                    javac -encoding UTF-8 -d ${CLASS_DIR} -cp "lib/junit-jupiter-api-5.11.4.jar;lib/junit-platform-console-standalone-1.9.3.jar" @sources.txt

                '''
            }
        }

        stage('Test') {
            steps {
                sh '''
                    echo "[*] Running tests with JUnit..."
                    java -jar lib/junit-platform-console-standalone-1.9.3.jar \
                        --class-path ${CLASS_DIR} \
                        --reports-dir ${REPORT_DIR} \
                        --disable-banner \
                        --details-theme=ascii \
                        --config junit.platform.output.capture.stdout=true \
                        --config junit.platform.reporting.open.xml.enabled=true \
                        > ${REPORT_DIR}/${TEST_OUTPUT}
                '''
            }
        }
    }

    post {
        always {
            echo "[*] Archiving test results..."
            junit "${REPORT_DIR}/**/*.xml"
            archiveArtifacts artifacts: "${REPORT_DIR}/**/*", allowEmptyArchive: true
        }
        success {
            echo "✅ Build and test succeeded"
        }
        failure {
            echo "❌ Build and test failed"
        }
    }
}
